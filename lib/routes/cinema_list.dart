import 'package:flutter/material.dart';

class CinemaList extends StatelessWidget {
  final String cityName;
  const CinemaList({required this.cityName, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
